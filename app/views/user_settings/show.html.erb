<h1>User Settings</h1>

<div>
  <strong>Email:</strong> <%= @user.email_address %>
</div>

<div>
  <strong>Role:</strong> <%= @user.role&.humanize || 'Standard' %>
</div>

<div>
  <strong>GitHub Token:</strong> 
  <% if @user.github_token.present? %>
    ••••••••••••••••
  <% else %>
    Not set
  <% end %>
</div>

<div>
  <strong>Allow tasks to access GitHub token:</strong> 
  <%= @user.allow_github_token_access ? "Yes" : "No" %>
</div>

<div>
  <strong>Global Agent Instructions:</strong>
  <% if @user.instructions.present? %>
    <pre class="instructions-preview"><%= @user.instructions %></pre>
    <small class="form-help">Configure mount path in individual agent settings.</small>
  <% else %>
    Not set
  <% end %>
</div>

<div>
  <strong>Git Configuration:</strong>
  <% if @user.git_config.present? %>
    <pre class="instructions-preview"><%= @user.git_config %></pre>
    <small class="form-help">Mounted as ~/.gitconfig in agent containers.</small>
  <% else %>
    Not set
  <% end %>
</div>

<div>
  <strong>Shrimp Mode 🍤:</strong> <%= @user.shrimp_mode? ? "Enabled" : "Disabled" %>
</div>

<p>
  <%= link_to 'Edit Settings', edit_user_settings_path, class: 'button' %>
  <%= link_to 'Change Email/Password', edit_account_settings_path, class: 'button' %>
</p>

<%= link_to 'Back', root_path %>